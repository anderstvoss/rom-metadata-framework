#!/usr/bin/env bash
set -euo pipefail

if gitleaks git --help >/dev/null 2>&1; then
    exec gitleaks git . \
        --no-banner \
        --redact
fi

exec gitleaks detect \
    --source . \
    --no-banner \
    --redact
